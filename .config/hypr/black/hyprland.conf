general {
    #col.active_border = rgb(ffffff) rgba(33333300) rgb(ffffff) rgba(33333300) rgb(ffffff) rgba(33333300) rgb(ffffff) 30deg
    col.active_border = rgb(ffffff) rgb(000000) rgb(ffffff) rgb(000000) rgb(ffffff) rgb(000000) rgb(ffffff) 30deg
    #col.inactive_border = rgb(222222)
    col.inactive_border = rgb(000000)
}

decoration {
    blur=1
    blur_size=3 # minimum 1
    blur_passes=2 # minimum 1, more passes = more resource intensive.
    # Your blur "amount" is blur_size * blur_passes, but high blur_size (over around 5-ish) will produce artifacts.
    # if you want heavy blur, you need to up the blur_passes.
    # the more passes, the more you can up the blur_size without noticing artifacts.
}
