require('telescope').setup{
  defaults = {
    mappings = {
      i = {
        [keys.telescope.which_key] = "which_key",
      },
      n = {
        [keys.telescope.which_key] = "which_key",
      }
    }
  },
}
